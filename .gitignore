# ── 資料與權重一律不進版控（規格要求）─────────────────────────
data/*
models/*
outputs/
!data/.gitkeep
!models/.gitkeep

# 產出物：摘要與圖表要進版控，README 的成果表才有依據可查。
# 只排除逐筆預測（檔案大、對讀者沒用）。
reports/predictions_*.jsonl

# make space 的暫存輸出
build/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.ipynb_checkpoints/

# 環境
.env
*.log

# OS
.DS_Store
Thumbs.db
